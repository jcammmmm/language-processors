funcion_principal
    seleccionar ( a ) entre
        caso 0 :
            imprimir(a);
        caso 1:
            imprimir(a*2);
            romper;
        defecto:
            a = a + 3;
            sqrt(a, 39);
    fin_seleccionar
fin_principal